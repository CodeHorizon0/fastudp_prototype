[project]
name = "fastudp"
version = "0.2.0"
description = "Production-oriented UDP transport layer with FastAPI-like routing and QUIC-inspired features"
requires-python = ">=3.11"
dependencies = [
  "cryptography>=42.0.0",
]

[project.scripts]
fastudp = "fastudp.cli:main"
